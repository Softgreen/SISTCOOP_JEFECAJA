'use strict';

// Configuring the Chat module
angular.module('usuario').run(['Menus',
    function (Menus) {
        // Set top bar menu items
        /*Menus.addMenuItem('topbar', {
            title: 'Usuario',
            state: 'usuario.app'
        });*/
    }
]);
